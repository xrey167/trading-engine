// Domain enums as `as const` maps — ported from quant-lib
// All symbols moved to their domain sub-modules; this file is now empty.
// Kept as a placeholder — delete once all direct importers are updated.
